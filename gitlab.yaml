global:
  hosts:
    domain: upodroid.com
    # hostSuffix: staging
    https: true
    externalIP: 35.246.37.36
    gitlab:
      name: gitlab.upodroid.com
      https: true
    registry:
      name: registry.upodroid.com
      https: true
    minio:
      name: minio.upodroid.com
      https: true

  redis:
    host: redis-master
    password:
      secret: redis
      key: redis-password

  psql:
    host: postgres-postgresql
    port: 5432
    database: gitlab
    username: postgres
    password:
      secret: postgres-postgresql
      key: postgresql-password

  smtp:
   enabled: true
   address: smtp.sendgrid.net
   port: 587
   user_name: apikey
   password:
     secret: sendgrid-apikey
     key: password
   tls: false
   starttls_auto: true
   openssl_verify_mode: peer
   authentication: login

  email:
   from: gitlab@upodroid.com
   display_name: GitLab

redis:
  enabled: false

certmanager-issuer:
 email: hi@upo.one 

gitlab:
  migrations:
    image.repository: registry.gitlab.com/gitlab-org/build/cng/gitlab-rails-ce
